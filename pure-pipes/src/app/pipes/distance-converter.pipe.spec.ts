import { DistanceConverterPipe } from './distance-converter.pipe';

describe('DistanceConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new DistanceConverterPipe();
    expect(pipe).toBeTruthy();
  });
});

import { UtilsPipePipe } from './utils-pipe.pipe';

describe('UtilsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new UtilsPipePipe();
    expect(pipe).toBeTruthy();
  });
});

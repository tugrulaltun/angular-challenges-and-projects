import { CustomForDirective } from './custom-for.directive';

describe('CustomForDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomForDirective();
    expect(directive).toBeTruthy();
  });
});

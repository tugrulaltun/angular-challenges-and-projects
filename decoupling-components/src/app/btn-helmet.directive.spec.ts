import { BtnHelmetDirective } from './btn-helmet.directive';

describe('BtnHelmetDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnHelmetDirective();
    expect(directive).toBeTruthy();
  });
});

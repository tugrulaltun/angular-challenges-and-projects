import { BtnBrainDirective } from './btn-brain.directive';

describe('BtnBrainDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnBrainDirective();
    expect(directive).toBeTruthy();
  });
});
